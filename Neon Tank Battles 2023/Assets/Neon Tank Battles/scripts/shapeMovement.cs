using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class shapeMovement : MonoBehaviour {

	int rotationSpeed = 0;
	void Start () {
		/*float x;
		float y;
		x = Random.Range (10, 30);
		y = Random.Range (10, 30);
		rotationSpeed = Random.Range (-4, 4);
		GetComponent<Rigidbody2D> ().AddForce (new Vector2(x / 1000, y / 1000));*/

	}

	void FixedUpdate() {
		//transform.Rotate (0, 0, rotationSpeed);
	}

}
